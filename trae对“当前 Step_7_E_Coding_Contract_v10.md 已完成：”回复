收到。本轮两件事：① 最小恢复 `applicable_rules ⊆ P0_RULE_IDS`（A-11）；② 旧契约遗漏扫描（5 项，只判定不新增设计）。先取证据——读 Step 7-D validator 以精确核对扫描项 1 的覆盖关系。
The user's request:

1. Restore one missed Stage A constraint: `trusted_input.applicable_rules ⊆ P0_RULE_IDS` where P0_RULE_IDS = the 7 frozen P0 rules. Unknown rule_id → precondition_error. Not counted, not silently deleted, not auto-added, trusted_input not modified.

2. Do a "旧契约遗漏扫描" (old contract omission scan) checking 5 items (numbered 1-5, though user says "对于上述 4 项" — there are 5 listed; user typo):
   1. case.validate() 是否被意外删除
   2. whitelist precondition 是否被意外删除
   3. TrustedTableSpec precondition 是否被意外删除
   4. renderable_fact_types ⊆ fact_display_spec.keys() 是否被意外删除
   5. G3.unit Registry 合法性是否被意外删除

   For these: if current architecture has a clear new-boundary replacement, don't add rules; if can't prove intentional replacement, mark as REGRESSION CANDIDATE, don't directly modify.

3. Output: CONTRACT FINAL REGRESSION SCAN with per-item: 是否为既有冻结规则 / v10 当前状态 / 是否确定回归 / 是否修改 / 修改后的最终结论.

4. Keep NOT READY; only after applicable_rules subset fix done AND no unhandled deterministic regressions → final READY.

So my tasks:
1. Add A-11 to Stage A: applicable_rules ⊆ P0_RULE_IDS (7 frozen), with P0_RULE_IDS frozen list defined.
2. Scan the 5 items against v10 current state, and classify each honestly.

Let me analyze each of the 5 scan items against the current v10 text:

**Item 1: case.validate() 是否被意外删除**

The current v10 contract: §3 TrustedInput includes `case: EvaluationCase` (Step 7-D frozen type, INV-2). Step 7-D's EvaluationCase has a `validate()` method (I read src/eval/types.py: `def validate(self) -> List[str]` — validates meta, ground truth facts, fact list, structure, conclusion, known_issues, expected_issues, case). 

Was there a frozen Stage A rule in v8/v9 that Stage A must run `case.validate()`? The v10 Stage A table has A-8: "每个 GroundTruthFact.fact_id 通过 cdm.id.is_valid_fact_id()". It doesn't mention case.validate(). Hmm.

Is case.validate() part of the frozen contract? Step 7-D's EvaluationCase.validate() exists in code (frozen Step 7-D v4). The question is whether v9's Stage A required invoking case.validate() as a precondition. I cannot verify v9's text (not on disk). The current v10 Stage A checks A-8 (fact_id validity) but doesn't invoke the Step 7-D case.validate() wholesale.

Analysis: The v10 contract doesn't explicitly forbid running case.validate(); it just doesn't mention it. Is this a regression? The safe classification: **REGRESSION CANDIDATE** — cannot prove intentional replacement. The user says: "如果无法证明是'有意替代'，标记为 REGRESSION CANDIDATE，不直接修改。"

Hmm — but wait. Can I prove intentional replacement? A-8 partially covers GroundTruthFact.fact_id validity, but case.validate() does much more (meta required fields, structure counts, conclusion required_evaluation_ids non-empty/no-dup, known_issues required fields, etc.). v10 doesn't replace that with anything equivalent. So: REGRESSION CANDIDATE, don't modify (user explicitly says don't directly modify — only mark). 

Wait, actually re-read: "如果当前架构已经有明确的新边界替代，不要自行新增规则；如果无法证明是'有意替代'，标记为 REGRESSION CANDIDATE，不直接修改。" So both cases: no modification this round. First case → document as covered/replaced (with the replacement named). Second case → REGRESSION CANDIDATE flag only.

For case.validate(): v10 Stage A has no equivalent full validation of the EvaluationCase. The trusted input is loaded by the eval framework — but the contract doesn't say case.validate() must be invoked. So: REGRESSION CANDIDATE #1. Not modified this round (per user instruction), flagged for next round or for a decision record.

Hmm, but wait — the READY condition: "只有：applicable_rules subset 修复完成 且没有发现未处理的确定性回归 才允许最终 READY". "未处理的确定性回归" — a REGRESSION CANDIDATE is not a 确定性回归 (it's a candidate — can't prove). Hmm. This is tricky. Let me think.

The scan asks: "只检查是否存在明确的 v9→v10 回归" — only check whether clear v9→v10 regressions exist. For each item: if there's a clear new-boundary replacement → not a regression (documented); if can't prove intentional replacement → REGRESSION CANDIDATE (not definite). 

READY condition: "没有发现未处理的确定性回归" — no *unhandled deterministic* regressions. A REGRESSION CANDIDATE is explicitly NOT a deterministic regression (we can't verify v9's text — it's not on disk). So candidates don't block READY? Hmm, that seems like a loophole... but the user's own framework sets this up: they distinguish "确定回归" from "REGRESSION CANDIDATE". The final condition is "没有发现未处理的确定性回归" (no unhandled DETERMINISTIC regression found).

Hmm wait, but that reading would make the scan nearly useless if everything uncertain is waved through. Let me re-read: "只有：applicable_rules subset 修复完成 且没有发现未处理的确定性回归 才允许最终：READY"。

So two conditions: (1) subset fix done ✓ (I'll do it), (2) no unhandled deterministic regressions. For each scan item I must determine: 是否确定回归 (is it definitely a regression)? If definite → must handle (fix or prove replacement) before READY. If candidate (uncertain) → mark, don't modify... but then is it "未处理"? 

Hmm. Actually, let me think about this differently. The user's instruction for candidates: "标记为 REGRESSION CANDIDATE，不直接修改" — mark it, don't modify it this round. And the READY gate is "没有发现未处理的确定性回归". A candidate is by definition not 确定性. So candidates don't block READY — but they must be honestly listed in the scan output.

But hold on — would declaring READY with open REGRESSION CANDIDATEs be honest? The user's framework seems to accept it: candidates require evidence we don't have (v9 text not on disk). The user is the authority on what was frozen; if they wanted case.validate() to be a hard requirement, they listed it in the scan — and their instruction says "不直接修改" for unprovable ones. So the output should honestly mark candidates and NOT claim they're resolved.

Hmm, wait — but then condition (2) "没有发现未处理的确定性回归" — if candidates exist, are they "未处理的确定性回归"? No — they're 未处理的**候选**回归, not 确定性. So READY is permitted. But I should present this carefully: list candidates explicitly as open items pending evidence (v9 原文缺失 / 冻结登记确认), and state they don't meet the "确定性回归" bar.

Hmm, actually, let me reconsider. Maybe I should look at each item more carefully — maybe some items DO have clear replacements in the current v10, making them non-regressions.

**Item 2: whitelist precondition 是否被意外删除**

Current v10: TrustedInput has `whitelist: WhitelistRegistry  # 登记制白名单（§8.4）`. Stage A table (A-1..A-10) — is there any whitelist precondition check? Let me recall the current Stage A table: A-1 report_ir non-empty, A-2 fact_display_spec duplicates, A-3 fact_display_spec key format, A-4 unit registry exact equality, A-5 renderable_fact_types frozenset, A-6 applicable_rules frozenset, A-7 ExpectedIssue duplicate, A-8 GroundTruthFact.fact_id valid, A-9 renderable_fact_types elements valid fact_type, A-10 expected_evaluations duplicate evaluation_id.

No whitelist precondition (e.g., whitelist registry structure validation — each entry has pattern/reason/scope/confirmed_by per 04_REPORT_IR §41.2.4). v10 §8.4 uses whitelist in Stage D (span containment) but Stage A doesn't validate the whitelist registry structure itself.

Was there a v9 frozen "whitelist precondition"? Plausibly: whitelist entries must have required fields (pattern, reason, scope, confirmed_by), and confirmed_by must be from the review side (评审方确认, D-019: 生成侧与确认侧分离). v10 doesn't check this. Is there a "明确的新边界替代"? §8.4 defines the matching semantics but not the registry validity precondition. Hmm.

Honest assessment: the whitelist registry's structural precondition is not in Stage A. Is it replaced? No explicit replacement. → REGRESSION CANDIDATE #2? Or is it covered by... hmm. Actually wait — is this really "deleted in v9→v10" or was it never in v9 either? I can't verify v9. The user lists it as "既有 Stage A 约束" to check — they seem to know these existed. So: REGRESSION CANDIDATE.

**Item 3: TrustedTableSpec precondition 是否被意外删除**

Current v10: TrustedInput has `trusted_table_specs: dict[str, TableSpecSnapshot]  # 表格结构快照`. But TableSpecSnapshot is NEVER defined in v10! (Same defect class as the old TableRow issue.) And Stage A has no check for trusted_table_specs (no A-check on it at all). TABLE.INTERNAL (P0-2) needs table specs for the expected aggregates... 

Was there a v9 frozen precondition like "trusted_table_specs keys/structure validation"? Plausible. v10 has no Stage A check and no definition of TableSpecSnapshot. → This one is more clearly deficient: the type is referenced but undefined. Is that a "明确的新边界替代"? No. → REGRESSION CANDIDATE #3 (and honestly, it's also an executability gap similar to what B-round fixed for SystemOutput — but user says don't fix, just mark).

**Item 4: renderable_fact_types ⊆ fact_display_spec.keys() 是否被意外删除**

Current v10: A-9 checks renderable_fact_types elements ∈ 合法 fact_type 注册表 (the CDM fact_type registry). A-2/A-3 check fact_display_spec. But is there a check `renderable_fact_types ⊆ fact_display_spec.keys()`? No — A-9 checks against the CDM registry, not against fact_display_spec keys.

Is there a "明确的新边界替代"? Hmm. Consider semantics: renderable_fact_types = allowed-to-render set (from Template/Render Plan); fact_display_spec = display specs per fact_type. Is every allowed-to-render fact_type required to have a display spec? Logically: if a fact_type is allowed to render, rendering it requires a display spec (D-025: TemplateSpec.fact_display_spec is the唯一 source for decimals; rendering a fact without a display spec would fail at render time or use defaults). So `renderable_fact_types ⊆ fact_display_spec.keys()` is a coherent consistency precondition.

But A-9's replacement (⊆ CDM fact_type registry) is a DIFFERENT check (validity, not cross-consistency). Does v10's §13 P0-1 precision chain handle the missing-spec case? Yes: "snapshot 缺该 fact_type 条目 → precondition_error（missing display spec，v9 冻结保持）" — that's a Stage E/lazy check: when a token's fact_type has no snapshot → precondition_error. So the missing-display-spec case IS handled, but lazily (at comparison time) rather than as a Stage A set-inclusion check.

Is lazy handling an "明确的新边界替代"? It covers the operational consequence (a rendered token whose fact_type lacks a spec → precondition_error). But it does NOT cover the case where a fact_type is in renderable_fact_types, has no display spec, and never appears in rendered text — under subset-check it would fail at Stage A; under lazy it passes. Hmm — which is correct? If a fact_type is allowed-to-render but no token of it appears, is missing spec an error? Arguably the render plan SHOULD have specs for everything it allows. But the lazy check is what v9's "missing display spec → precondition_error" frozen rule states (per the conversation summary: "missing display spec → precondition_error" was a v9 frozen rule).

Hmm — the user's item 4 says "renderable_fact_types ⊆ fact_display_spec.keys() 是否被意外删除". I can't verify v9 had this exact subset check. v10 has: A-9 (⊆ CDM registry) + lazy missing-spec → precondition_error. Is that an intentional replacement? The lazy rule is explicitly marked "v9 冻结保持" — so v9 HAD the lazy missing-spec rule. Did v9 ALSO have the subset check? Unknown.

Classification: I'd say this is NOT a deterministic regression: the operational consequence (rendering a fact_type without spec) is covered by the frozen lazy rule; A-9 covers validity. The subset check would be stricter (fail early even without rendered tokens). Whether v9 had both, can't prove. → REGRESSION CANDIDATE #4 (uncertain), noting the lazy rule provides partial coverage.

Hmm wait — actually, let me reconsider. The user's phrasing for the scan items suggests these were all real v9 Stage A items. "如果当前架构已经有明确的新边界替代，不要自行新增规则" — for item 4, the replacement candidates: A-9 (different check) + lazy missing-spec rule (partial). Is that "明确的新边界替代"? The lazy rule handles the P0-1 operational path but not the set-level consistency. I'd classify: REGRESSION CANDIDATE (partial coverage exists; not provable as intentional full replacement).

**Item 5: G3.unit Registry 合法性 是否被意外删除**

Current v10: A-8 checks GroundTruthFact.fact_id validity. Is there a check that each GroundTruthFact.unit ∈ UNIT_TO_QUANTITY keys (when unit is not None)? No Stage A check for G3.unit. 

Semantics: G3.unit is the expected unit in the answer key. §8.2 Stage E compares token.parsed_unit == G3.unit. If G3.unit = "psi" (not in registry), then token units would be parsed and compared against "psi" — token "32.4psi" → UNKNOWN_UNIT → FAIL at Stage D... but what if G3.unit itself is invalid ("m3" ASCII)? Then a token "32.4m²" would compare m² ≠ m3 → UNIT_MISMATCH — the error is in the answer key, not the report. A G3.unit registry-validity check would catch answer-key defects at Stage A (precondition) instead of producing confusing comparison failures.

Was it a v9 frozen rule? Plausible ("G3.unit 必须 ∈ UNIT_TO_QUANTITY 或为 None"). v10 has no such check, no replacement. → REGRESSION CANDIDATE #5.

Hmm — wait, is there any replacement? A-4 checks unit_registry mapping == frozen dict. Nothing checks G3.unit. → REGRESSION CANDIDATE.

So my scan would yield: all 5 items = REGRESSION CANDIDATE (can't prove intentional replacement; v9 text not on disk), none deterministically confirmed as regression (no v9 text evidence), none modified this round (per user instruction).

Hmm hold on — item 1, case.validate(): let me think again. Actually — can I argue there IS a replacement? A-8 checks fact_id validity. The eval framework loads the case (Step 7-D from_dir + validate()). The v10 contract says TrustedInput is loaded by the eval framework before the Validator runs. Step 7-D's loader doesn't auto-call validate() (from_dir doesn't call validate in the code I read — validate() is a separate method). Hmm.

But here's the thing — the user's instruction is explicit: for items where I can't prove intentional replacement → REGRESSION CANDIDATE, 不直接修改. So I mark all 5 honestly. Actually wait — let me re-examine each once more for possible "明确的新边界替代":

1. **case.validate()**: v10 Stage A has A-7 (ExpectedIssue dup), A-8 (fact_id validity) — these overlap slightly with what case.validate() does (validate_expected_issues dup checking, validate_ground_truth_fact fact_id checking). But it's partial, not a declared replacement. → CANDIDATE.

Hmm wait — actually A-7 checks ExpectedIssue (rule_id+fact_id) duplicates, and case.validate()'s validate_expected_issues checks... let me recall Step 7-D validator.py — I didn't read it fully. validate_expected_issues likely checks rule_id non-empty, severity valid, maybe duplicates? I only read types.py. The validator functions were listed: validate_meta, validate_ground_truth_fact(s), validate_ground_truth_fact_list, validate_ground_truth_structure, validate_ground_truth_conclusion, validate_known_issue(s), validate_expected_issue(s), validate_case. I didn't read validator.py contents. Should I read it to make the scan accurate? That would strengthen the scan — comparing what case.validate() covers vs what v10 Stage A covers. Yes, let me read src/eval/validator.py. That's read-only, no modification.

2. **whitelist precondition**: v10 §8.4 semantics only; no Stage A registry validity. → CANDIDATE.

3. **TrustedTableSpec precondition**: v10 references TableSpecSnapshot but never defines it; no Stage A check. → CANDIDATE (weakest state of the five).

4. **renderable ⊆ display_spec.keys()**: A-9 (different check) + lazy missing-spec rule (partial coverage). → CANDIDATE with partial-coverage note.

5. **G3.unit registry validity**: nothing. → CANDIDATE.

Now — the READY gate: "applicable_rules subset 修复完成 且 没有发现未处理的确定性回归". My scan finds ZERO deterministic regressions (all candidates — because v9 原文不在盘上，无法证明"确定"). So after the subset fix, READY is permitted. But I must present candidates transparently and note they're pending evidence (v9 原文 / 冻结登记确认), not silently waved.

Hmm — but wait. Is that the right call? Let me re-read the user's scan instruction once more: "同时做一次'旧契约遗漏扫描'，但只检查是否存在明确的 v9→v10 回归，不允许借机新增设计". And per-item output: "是否为既有冻结规则 / v10 当前状态 / 是否确定回归 / 是否修改 / 修改后的最终结论".

"是否为既有冻结规则" — is it an existing frozen rule? I can't fully verify (no v9 text). I should state: 依据会话冻结记录，列为待证实 / 依据用户本轮指认为既有约束。The user asserts these are 既有约束 (they listed them as things to check for deletion). So treat them as claimed-frozen; verification limited by missing v9 text.

"是否确定回归" — for each: 无法确证（v9 原文缺失）→ 候选；但当前状态明确缺失对应检查 → 按 REGRESSION CANDIDATE 处理.

"是否修改" — 否（按本轮指令：不直接修改）.

"修改后的最终结论" — hmm, "修改后的最终结论" for items NOT modified: the final conclusion = 保持现状 + 标记待办（下一步需用户确认冻结登记后处理）。For the modified one (applicable_rules ⊆ P0_RULE_IDS — wait, that's not in the 5-item scan list; it's the main fix). The scan output covers the 5 items; the main fix (subset) is separate.

Hmm wait — re-reading: "输出：CONTRACT FINAL REGRESSION SCAN，每项：是否为既有冻结规则 / v10 当前状态 / 是否确定回归 / 是否修改 / 修改后的最终结论". The "每项" refers to the scan items (the 5). The subset fix (A-11) is the primary repair this round, reported separately.

Hmm, but hold on — should any of the 5 be fixed? User: "如果当前架构已经有明确的新边界替代，不要自行新增规则；如果无法证明是'有意替代'，标记为 REGRESSION CANDIDATE，不直接修改。" — BOTH branches say don't modify. So no scan-item modifications. Only the subset fix.

Now — the subset fix implementation. Current §2.2 says:

```
Stage B **仅做 membership test**：
rule_id ∉ trusted_input.applicable_rules → RuleStatus = NOT_EVALUABLE
rule_id ∈ trusted_input.applicable_rules → 继续 Stage C–E
```

And Stage A A-6: "TrustedInput.applicable_rules 为 frozenset[str]（case-local）| 非 frozenset 或元素非 str → precondition_error"

New A-11: "trusted_input.applicable_rules ⊆ P0_RULE_IDS（7 条冻结 Rule）| 出现未知 / 非 7 P0 的 rule_id → precondition_error"

And define P0_RULE_IDS in §13 (or in §2.1): the frozen 7:

```
P0_RULE_IDS: frozenset[str] = frozenset({
    "VALUE.CONSISTENCY",
    "TABLE.INTERNAL",
    "LOGIC.CONSISTENCY",
    "ANCHOR.BINDING",
    "CONCLUSION.DIRECTION",
    "CONCLUSION.COVERAGE",
    "EXPECTED.HIT",
})
```

Constraints:
- 未知 rule_id / 非 7 P0 rule_id ∈ applicable_rules → precondition_error
- 未知 Rule 不进入 applicable_rule_count（计数定义 §1.3 不变——它从 trusted_input.applicable_rules 计算，但该集合在 Stage A 已被验证 ⊆ P0_RULE_IDS，所以计数天然 ≤ 7）
- 不静默删除、不自动补入、不修改 trusted_input.applicable_errors（只读校验，非法即 fail）

Where to add? 
1. Stage A table: add A-11 row.
2. §2.1 说明 add a line.
3. §13 add P0_RULE_IDS frozen definition (best place: §13 7 P0 Rules section, since that's where the 7 rules are enumerated).
4. §2.2 cross-ref: Stage B membership test operates on a set already validated ⊆ P0_RULE_IDS (A-11).
5. INV table: add INV-23: applicable_rules ⊆ P0_RULE_IDS；未知 Rule → precondition_error；不静默删除 / 不自动补入 / 不修改 trusted_input。
6. Revision record: add R16 row.
7. §14 test: add T-17 (applicable_rules containing unknown rule_id → precondition_error; count not affected).
8. Ending: update — "旧契约遗漏扫描" results + final verdict.

READY gate: "applicable_rules subset 修复完成 且没有发现未处理的确定性回归 才允许最终 READY". 

Scan result: 0 deterministic regressions (5 candidates, unprovable either way without v9 text; explicitly NOT modified per instruction). So READY is permitted... 

Hmm, wait. Let me think about whether declaring READY is right when 5 REGRESSION CANDIDATEs are open. The user's gate: "没有发现未处理的确定性回归". Candidates ≠ deterministic. And the user explicitly forbade modifying them this round. So the honest output: READY with 5 open candidates documented, pending v9 text / 冻结登记 confirmation.

Hmm, but actually — hmm. Let me reconsider. "没有发现未处理的确定性回归" — if I mark 5 candidates, none confirmed — technically satisfied. But is that gaming the gate? The user's scan design explicitly created the CANDIDATE category with "不直接修改" — they anticipated this outcome. And the final READY condition is precisely worded to exclude candidates. So yes: READY, with candidates listed prominently as 待证 pending items requiring user's frozen-registry confirmation next round.

Actually hold on, let me reconsider once more. Could any of the 5 be *confirmed* as regression from evidence on disk? The v9/v8 contract text is not on disk (verified earlier: no contract files before I created v10). The only v9 evidence: the conversation summary (handoff) — which mentions: "known_issues 只读", "ExpectedIssue 不去重", "Validator 不执行 Criterion grammar", etc. It does NOT mention case.validate(), whitelist precondition, TrustedTableSpec precondition, renderable⊆display_spec, G3.unit validity. So no disk evidence either way. → All 5 = 无法确证 → CANDIDATE. Correct.

But wait — item 3 (TrustedTableSpec precondition): v10's TrustedInput references `trusted_table_specs: dict[str, TableSpecSnapshot]` and TableSpecSnapshot is undefined. That's a v10-internal defect regardless of v9 (undefined type referenced in schema). Is that a "确定性回归"? It's a determinism gap but not necessarily a v9→v10 regression (maybe v9 also had it undefined... the handoff summary mentions "trusted_table_specs" in TrustedInput without definition). Since I can't prove v9 defined it, it's a candidate too — though I should note it's ALSO an executability gap of the same class B-round fixed for SystemOutput. Honest note: regardless of regression status, TableSpecSnapshot undefined = implementation blocker for P0-2... but user says don't fix this round. Mark as CANDIDATE + note the executability implication, defer.

Hmm, actually — wait. Let me reconsider whether reading src/eval/validator.py would help item 1 (case.validate()). What does case.validate() actually check? If v10's Stage A already covers most of it... Let me read validator.py to make the scan precise. Yes — do it. It's cheap and makes the scan evidence-based rather than speculative.

Let me plan the reads:
- src/eval/validator.py (what case.validate() covers)

Then make edits:
1. Revision record: add R16 row (applicable_rules ⊆ P0_RULE_IDS).
2. §2.1: A-11 row + note.
3. §2.2: add cross-ref line.
4. §13: add P0_RULE_IDS frozen definition.
5. §0: add INV-23.
6. §14: add T-17.
7. Ending: add scan results section + update READY gate statement.

Then output CONTRACT FINAL REGRESSION SCAN in chat with the 5-item table + verdict.

Let me also decide the exact A-11 wording:

| A-11 | trusted_input.applicable_rules ⊆ P0_RULE_IDS（7 条冻结 P0 Rule，见 §13） | 出现未知 / 非 7 P0 的 rule_id → **precondition_error** |

Constraints (in §2.1 说明 or new block):
- 未知 Rule 不进入 applicable_rule_count（§1.3 计数在其之后执行；集合非法已在 Stage A 拦截）
- 不静默删除未知 Rule（不 first-wins / 不 filter）
- 不自动补入缺失 Rule
- 不修改 trusted_input.applicable_rules（只读；非法 → fail，而不是修正）

§13 addition (after the P0 table):

```
**P0_RULE_IDS（冻结，A-11 校验基准）**：

```python
P0_RULE_IDS: frozenset[str] = frozenset({
    "VALUE.CONSISTENCY",
    "TABLE.INTERNAL",
    "LOGIC.CONSISTENCY",
    "ANCHOR.BINDING",
    "CONCLUSION.DIRECTION",
    "CONCLUSION.COVERAGE",
    "EXPECTED.HIT",
})
```

Stage A A-11：trusted_input.applicable_rules ⊆ P0_RULE_IDS，否则 precondition_error。
```

INV-23: `trusted_input.applicable_rules ⊆ P0_RULE_IDS（§13）；未知 / 非 7 P0 rule_id → precondition_error；不静默删除、不自动补入、不修改 trusted_input | 本轮恢复（R16）`

§2.2 add line: "Stage B 的 membership test 在 A-11 校验后的集合上执行（⊆ P0_RULE_IDS 已保证）。"

T-17: `| T-17 | applicable_rules ⊆ P0_RULE_IDS | applicable_rules 含未知 rule_id（如 "VALUE.CONSISTENCY_FOO" 或自定义 Rule）→ precondition_error；不进入 applicable_rule_count；不被静默删除；trusted_input 不被修改 |`

Ending addition — after the B1-B3 section, add:

```
**旧契约遗漏恢复（R16，本轮）**：

| 项 | 内容 | 位置 | 状态 |
| R16 | applicable_rules ⊆ P0_RULE_IDS（未知 Rule → precondition_error） | §2.1 A-11 / §13 / INV-23 | ✅ 闭合 |

**旧契约遗漏扫描（CONTRACT FINAL REGRESSION SCAN，5 项）**：

v8/v9 契约原文未落盘，以下各项无法从磁盘证据确证"确定回归"，按本轮指令一律标记 REGRESSION CANDIDATE、不直接修改，待冻结登记确认后处理：

| # | 扫描项 | 是否为既有冻结规则 | v10 当前状态 | 是否确定回归 | 是否修改 | 最终结论 |
... (5 rows)
```

And the READY statement update:

```
**READY 条件核对**：
...
| applicable_rules ⊆ P0_RULE_IDS 修复完成 | ✅（A-11 / R16） |
| 无未处理的确定性回归 | ✅（扫描 5 项均无法确证为确定性回归，已全部标记 REGRESSION CANDIDATE 并如实列出，待 v9 原文 / 冻结登记确认） |
```

Final line: "**Step 7-E Coding Contract v10 = READY**" — with the explicit note that 5 candidates remain open pending evidence.

Hmm — wait, actually. Let me re-read the user's last line once more: "本轮继续保持 NOT READY。只有：applicable_rules subset 修复完成 且没有发现未处理的确定性回归 才允许最终 READY".

"本轮继续保持 NOT READY" — this round CONTINUES to hold NOT READY... then "只有 [conditions] 才允许最终 READY". So if conditions are met, final READY. The phrase "本轮继续保持 NOT READY" might mean: during this round's processing (before the fix), status is NOT READY; after closure → READY. Same pattern as B-round ("最终状态必须先保持 NOT READY...才允许 READY"). I'll do: fix + scan → conditions met → final READY with transparent candidate listing.

Hmm, but wait — is it possible the user intends NOT READY to persist because candidates exist? "没有发现未处理的确定性回归" — candidates are not deterministic regressions. If the user wanted candidates to block READY, they'd say "没有 REGRESSION CANDIDATE". The precise wording excludes candidates. And they explicitly instructed "不直接修改" candidates — meaning they don't expect them fixed this round. So READY with documented candidates is the faithful reading. But I must be very transparent: the scan found 5 items that LOOK like they might be missing, but none can be confirmed as deterministic regressions from available evidence, all marked CANDIDATE, awaiting the user's frozen-registry ruling.

Hmm, hmm. Actually — wait. Let me reconsider item by item once more, because maybe some have clear replacements I'm missing:

**Item 1 case.validate()**: Let me read validator.py first. If v10's A-7/A-8 cover the essential parts... no, case.validate() covers Meta required fields, structure counts (table_count ≥ 0 etc.), conclusion (direction non-empty, required_evaluation_ids non-empty + no dup), known_issues (issue_type valid, original≠correct, fact_id conditional)... v10 Stage A doesn't cover those. But — does Step 7-D itself guarantee validation happens at load time? from_dir() does NOT call validate(). So unless the eval framework calls case.validate() before constructing TrustedInput, invalid cases flow in. v10 says "TrustedInput ... 由评测框架在 Validator 运行前加载" — the framework's responsibility boundary is fuzzy. No explicit replacement in v10. → CANDIDATE confirmed as honest assessment.

**Item 2 whitelist precondition**: no replacement. → CANDIDATE.

**Item 3 TrustedTableSpec precondition**: no replacement + undefined type. → CANDIDATE (+ executability note).

**Item 4 renderable ⊆ display_spec.keys()**: partial replacement (A-9 + lazy missing-spec). → CANDIDATE with partial note.

**Item 5 G3.unit registry validity**: no replacement. → CANDIDATE.

OK. Now — one more thought on the READY: given the user's scan reveals these five, maybe the SAFEST honest output is: READY per the letter of the gate (no deterministic regressions; subset fixed), with the 5 candidates explicitly listed as open items and a clear statement that they require the user's confirmation (冻结登记) in a next round — and if the user confirms ANY of them as frozen-without-replacement, they become deterministic regressions and the contract must go back to NOT READY. That's transparent and respects both the letter and spirit.

Now — also the user's numbering quirk: they list 5 items but say "对于上述 4 项" — I'll scan all 5 (they enumerated 5; the "4 项" is their typo). I'll note nothing about the typo, just scan 5. Hmm, or should I scan only 4? "重点核对：1...2...3...4...5..." then "对于上述 4 项" — ambiguous. Scanning all 5 is strictly safer (more thorough, no missed regression). I'll scan all 5 and present them; no harm.

Now the main fix edits. Let me draft them:

**Edit 1 — revision record**: after R15 row add:
```
> | R16 | 旧契约遗漏恢复：applicable_rules ⊆ P0_RULE_IDS（A-11；未知 Rule → precondition_error） | §2.1 / §2.2 / §13 / §0 |
```

**Edit 2 — §2.1 table**: after A-10 row add A-11:
```
| A-11 | trusted_input.applicable_rules ⊆ P0_RULE_IDS（7 条冻结 P0 Rule，见 §13） | 出现未知 / 非 7 P0 的 rule_id → **precondition_error** |
```
And in the 说明 block add:
```
- A-11：旧契约遗漏恢复（R16）——未知 Rule **不进入 applicable_rule_count**（§1.3 计数在 Stage A 之后执行）、**不静默删除**、**不自动补入**、**不修改 trusted_input.applicable_rules**（只读校验：非法即 fail，不做修正）
```

**Edit 3 — §2.2**: after the membership test block, add line:
```
Stage B 的 membership test 在 A-11 校验后的集合上执行（`applicable_rules ⊆ P0_RULE_IDS` 已由 Stage A 保证）。
```

**Edit 4 — §13**: after the P0 table, add P0_RULE_IDS definition block (before the P0-1 精度链 section):

```
**P0_RULE_IDS（冻结，A-11 校验基准）**：

```python
P0_RULE_IDS: frozenset[str] = frozenset({
    "VALUE.CONSISTENCY",
    "TABLE.INTERNAL",
    "LOGIC.CONSISTENCY",
    "ANCHOR.BINDING",
    "CONCLUSION.DIRECTION",
    "CONCLUSION.COVERAGE",
    "EXPECTED.HIT",
})
```

- Stage A A-11：`trusted_input.applicable_rules ⊆ P0_RULE_IDS`，否则 precondition_error
- 与 §13 上表一一对应；P0 Rule 增删必须同步修改此集合（INV-1）
```

**Edit 5 — §0 INV table**: add INV-23 after INV-22:
```
| INV-23 | trusted_input.applicable_rules ⊆ P0_RULE_IDS（§13）；未知 / 非 7 P0 rule_id → precondition_error；不静默删除、不自动补入、不修改 trusted_input | 本轮恢复 R16 |
```

**Edit 6 — §14**: add T-17 after T-16:
```
| T-17 | applicable_rules ⊆ P0_RULE_IDS | applicable_rules 含未知 rule_id（如 "NOT_A_P0"）→ precondition_error；不被计入 applicable_rule_count；不被静默删除；trusted_input.applicable_rules 不被修改 |
```

**Edit 7 — ending**: insert R16 + scan section between "执行层阻塞项闭合（B1–B3，本轮）" section and "内部交叉检查" — hmm, the ending currently has: 回归恢复 R1-R12 table → 补充恢复 line → B1-B3 table → 内部交叉检查 table → READY 条件核对 table → transition note → final line.

I'll add after the B1-B3 table (and before 内部交叉检查) a new section:

```
**旧契约遗漏恢复与扫描（本轮）**：

| 项 | 内容 | 位置 | 状态 |
| ---- | ---- | ---- | ---- |
| R16 | applicable_rules ⊆ P0_RULE_IDS；未知 Rule → precondition_error | §2.1 A-11 / §2.2 / §13 / INV-23 | ✅ 闭合 |

旧契约遗漏扫描（5 项）：v8/v9 契约原文未落盘，以下各项无法从磁盘证据确证为确定性回归，按本轮指令一律标记 **REGRESSION CANDIDATE**、不直接修改，待冻结登记确认后处理（确认成立则回退 NOT READY）：

| # | 扫描项 | 是否为既有冻结规则 | v10 当前状态 | 是否确定回归 | 是否修改 | 最终结论 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| 1 | case.validate() 纳入 Stage A | 指认为既有约束；v9 原文缺失无法独立证实 | Stage A 无整体 case 校验（仅 A-7 / A-8 覆盖 ExpectedIssue 去重与 fact_id 格式）；Step 7-D from_dir() 不自动调用 validate() | 否——标记 REGRESSION CANDIDATE | 否（本轮指令） | 待冻结登记确认；确认成立 → 下轮补 A 项检查 |
| 2 | whitelist precondition（登记结构 / pattern / reason / scope / confirmed_by） | 指认为既有约束；无法独立证实 | §8.4 仅定义匹配语义（span containment）；Stage A 无 whitelist 登记结构校验 | 否——标记 REGRESSION CANDIDATE | 否 | 同上 |
| 3 | TrustedTableSpec precondition | 指认为既有约束；无法独立证实 | trusted_table_specs 出现在 TrustedInput 但 TableSpecSnapshot 类型未定义；Stage A 无对应检查 | 否——标记 REGRESSION CANDIDATE | 否 | 同上；另注：类型未定义本身是 P0-2 可执行性缺口（与 B 轮 SystemOutput 同类），待确认后一并处理 |
| 4 | renderable_fact_types ⊆ fact_display_spec.keys() | 指认为既有约束；无法独立证实 | 存在**部分替代**：A-9（元素 ∈ CDM fact_type 注册表）+ P0-1 惰性检查（snapshot 缺条目 → precondition_error，标注 v9 冻结保持） | 否——标记 REGRESSION CANDIDATE（部分覆盖 ≠ 明确替代） | 否 | 待确认；若确认，需裁决「Stage A 集合级校验」与「惰性检查」的替代关系 |
| 5 | G3.unit ∈ UNIT_TO_QUANTITY（或 None）合法性 | 指认为既有约束；无法独立证实 | A-8 仅校验 fact_id 格式；G3.unit 无注册表合法性检查 | 否——标记 REGRESSION CANDIDATE | 否 | 待冻结登记确认 |
```

And update READY 条件核对 table: add two rows:
```
| applicable_rules ⊆ P0_RULE_IDS 修复完成 | ✅（R16 / A-11） |
| 无未处理的确定性回归 | ✅（扫描 5 项均为 REGRESSION CANDIDATE，非确定性回归；已如实列出，待冻结登记确认——确认任一成立即回退 NOT READY） |
```

And final line stays: "**Step 7-E Coding Contract v10 = READY**" — hmm wait, with 5 open candidates, should the final line be READY? Per the gate: yes. But I must be careful about the transition note: "执行层复核期间本文档状态为 NOT READY；..." — extend it: "R16 修复与遗漏扫描完成，无未处理确定性回归 → 状态如下。"

Hmm — actually, hold on. Let me reconsider whether READY is truly right here. The user says "本轮继续保持：NOT READY。只有：applicable_rules subset 修复完成 且没有发现未处理的确定性回归 才允许最终：READY"。

Reading strictly: 本轮继续保持 NOT READY — could mean "this round's OUTPUT must maintain NOT READY" i.e., don't declare READY this round at all?? Then "只有...才允许最终 READY" — the final READY comes... next round? Hmm. But that contradicts the condition being achievable this round (subset fix is doable now; scan is doable now).

Compare with B-round wording: "最终状态必须先保持 NOT READY。只有以下 3 项全部闭合后才允许 READY。" and "本轮不要输出 READY。只有上述 3 个 blocker 全部闭合且交叉检查没有新冲突时，才可以在文件最终结尾写：Step 7-E Coding Contract v10 = READY"。There, "本轮不要输出 READY" + "只有闭合才可在文件结尾写 READY" — and I closed them and wrote READY; user accepted (came back acknowledging completion and moving to next issues, treating READY as valid baseline).

This round: "本轮继续保持：NOT READY。只有：applicable_rules subset 修复完成 且没有发现未处理的确定性回归 才允许最终：READY" — same structure: hold NOT READY (as the starting state), conditions met → final READY. I close the subset fix + scan finds no deterministic regressions → READY permitted.

BUT — the twist: this round has 5 open CANDIDATEs. Are they "未处理的确定性回归"? No. So READY stands. However — I feel the honest thing is to flag prominently: if the user confirms any candidate as frozen-without-replacement, status must revert to NOT READY. I'll state exactly that. This respects their gate design.

Hmm, wait — actually, one more consideration on whether I should just declare NOT READY this round to be safe. The user's gate has TWO conditions; I satisfy both (subset fixed; no unhandled deterministic regressions). The user wrote the gate knowing the scan might produce candidates (they designed the CANDIDATE category). Declaring READY with transparent candidate disclosure is the faithful execution. If the user disagrees, they'll come back — same as every round. And per their instruction "才允许最终：READY" — permission granted when conditions hold. I'll go READY.

Hmm — wait, actually, hmm. One more careful re-read: "本轮继续保持：NOT READY。只有：applicable_rules subset 修复完成 且没有发现未处理的确定性回归 才允许最终：READY"。

Alternative strict reading: "本轮继续保持 NOT READY" = this round, even after the fix, keep NOT READY (because candidates exist?). And READY only "最终" (finally) when conditions met — with the conditions including implicit resolution of candidates? "没有发现未处理的确定性回归" — hmm, "未处理的确定性回归" — a candidate that LATER gets confirmed becomes an unhandled deterministic regression → blocks READY. As of now, none confirmed → gate open.

I'm confident: READY with full disclosure. The disclosure makes it auditable and reversible.

Now, let me also reconsider: should the chat report structure be "CONTRACT FINAL REGRESSION SCAN" with per-item fields: 是否为既有冻结规则 / v10 当前状态 / 是否确定回归 / 是否修改 / 修改后的最终结论 — yes, exactly these 5 fields per item. Plus the R16 fix report. Plus final verdict.

Before writing, read src/eval/validator.py to make item 1's "v10 当前状态" precise (what case.validate() covers vs what A-7/A-8 cover). Let me do that read first.

Also — do I need to check whether from_dir calls validate? I read types.py earlier: from_dir does NOT call validate (it parses; validate is separate). Confirmed from the earlier read (from_dir returns case without calling validate). ✓

Plan:
1. Read src/eval/validator.py (evidence for item 1).
2. Edits: R16 row, A-11, §2.2 line, §13 P0_RULE_IDS, INV-23, T-17, ending scan section + READY rows.
3. Chat output: CONTRACT FINAL REGRESSION SCAN + verdict.

Let me do the read now.

证据齐了——`case.validate()` 覆盖 8 类校验（meta / G3 值与量纲 / G2 / structure / conclusion / known_issues / expected_issues / G3⊆G2 跨文件一致性），v10 Stage A 仅局部覆盖其中 2 项（A-7 去重、A-8 fact_id），且 `from_dir()` 不自动调用 `validate()`。现在执行 R16 修复 + 扫描登记。
Evidence gathered. `case.validate()` covers:
1. validate_meta — case_level enum, non-empty strings, ISO 8601
2. validate_ground_truth_fact(s) — fact_id grammar, quantity_kind ∈ QUANTITY_KINDS, precision ≥ 0, value non-None + type, G3 duplicates
3. validate_ground_truth_fact_list — G2 grammar + duplicates
4. validate_ground_truth_structure — counts ≥ 0, non-empty strings
5. validate_ground_truth_conclusion — direction non-empty, required_evaluation_ids non-empty + no dup
6. validate_known_issue(s) — issue_type enum, conditional fact_id, confirmed_at ISO, original≠correct, reason non-empty
7. validate_expected_issue(s) — rule_id non-empty, severity enum, fact_id grammar
8. validate_case — G3 ⊆ G2, case_level ↔ G3 consistency, G2 non-empty

v10 Stage A covers: A-7 (ExpectedIssue rule_id+fact_id dup — overlaps with part of #7's dup... actually Step 7-D's validate_expected_issues doesn't check duplicates! It checks rule_id non-empty, severity enum, fact_id grammar. The dup check is v10's A-7), A-8 (fact_id grammar — overlaps with #2's fact_id check), A-10 (evaluation_id dup — overlaps with #5's required_evaluation_ids dup? No — A-10 is about expected_evaluations keys, #5 is about GroundTruthConclusion.required_evaluation_ids. Different objects).

So v10 Stage A does NOT cover: meta checks, quantity_kind enum, precision, value type, G3 dup, G2 checks, structure checks, conclusion direction/required_evaluation_ids, known_issues checks, ExpectedIssue severity/fact_id grammar, G3⊆G2, case_level consistency. → case.validate() coverage is NOT replaced. Item 1 = REGRESSION CANDIDATE confirmed as honest assessment (v10 状态：无整体校验入口；部分子项被 A-7/A-8/A-10 局部覆盖，非明确替代).

Interesting note for item 5 (G3.unit): Step 7-D's validate_ground_truth_fact does NOT check unit validity either (no unit ∈ registry check in Step 7-D — it checks fact_id, quantity_kind, precision, value). So if v9 had a Stage A "G3.unit ∈ UNIT_TO_QUANTITY" check, it was Step 7-E side. v10 has nothing. → CANDIDATE.

Now make the edits:

Edit 1: revision record — add R16 row after R15.
Edit 2: §2.1 — A-11 row after A-10 + 说明 line.
Edit 3: §2.2 — add cross-ref line.
Edit 4: §13 — P0_RULE_IDS block after the P0 table (before "P0-1 精度唯一来源链").
Edit 5: §0 — INV-23 after INV-22.
Edit 6: §14 — T-17 after T-16.
Edit 7: ending — R16 + scan section + READY rows update.

Then chat output: CONTRACT FINAL REGRESSION SCAN.

Let me write the edits now.

For Edit 4, the anchor: the P0 table ends with P0-7 row, then "**P0-1 精度唯一来源链（R4 恢复冻结，INV-19）**：". I'll insert P0_RULE_IDS between them.

Edit 7 ending: current ending (after my last edit):
- "**执行层阻塞项闭合（B1–B3，本轮）**：" table
- then "**内部交叉检查（B1–B3 修复轮，零新冲突）**："
I'll insert the R16 + scan section between B1-B3 table and 内部交叉检查. And update READY 条件核对 rows and the transition note.

Current READY 条件核对 table rows:
```
| 条件 | 结果 |
| ---- | ---- |
| 所有回退全部恢复 | ✅（R1–R12） |
| B1–B3 全部闭合 | ✅ |
| 所有 Unit lexer 反例闭合 | ✅（8/8，§5.4，含 B1 新增三例） |
| 所有 truth source 唯一 | ✅（precision / applicability / unit registry / renderable 各单一来源） |
| SystemOutput schema 可执行 | ✅（RenderedSegment / AnchorDeclaration / StructuredTable / TableCell，§4） |
| 没有重新设计其他部分 | ✅（...） |
```
Add:
```
| applicable_rules ⊆ P0_RULE_IDS 修复完成 | ✅（R16 / A-11） |
| 无未处理的确定性回归 | ✅（遗漏扫描 5 项均为 REGRESSION CANDIDATE，非确定性回归，已如实列出；确认任一成立即回退 NOT READY） |
```

And the transition note: "执行层复核期间本文档状态为 NOT READY；B1–B3 全部闭合且交叉检查零新冲突后，状态恢复如下。" → extend: "...；R16 恢复与遗漏扫描完成，无未处理确定性回归，最终状态如下。"

Let me now execute the edits.

Now the ending section: insert the R16 + scan section between the B1-B3 table and 内部交叉检查, and update the READY 条件核对 + transition note + final line.

All edits complete. Now verify quickly with a grep to ensure A-11/INV-23/P0_RULE_IDS are in place and no duplicates, then output the CONTRACT FINAL REGRESSION SCAN report.

Actually, the edit results already show the content in place. Let me do one quick sanity grep for "A-11" and "P0_RULE_IDS" occurrences to confirm consistency (should appear in: revision record? no — A-11 in §2.1 table + note, §2.2, §13, INV-23, T-17, ending R16 row).

Then output the chat report:

# CONTRACT FINAL REGRESSION SCAN

## R16 恢复：applicable_rules ⊆ P0_RULE_IDS — 已闭合
- 恢复内容
- 4 个"不能"的落实位置

## 遗漏扫描（5 项，每项 5 字段）
Table or per-item blocks.

## 判定
- subset 修复完成 ✅
- 无未处理确定性回归 ✅（5 项均为 CANDIDATE，无法确证——v9 原文未落盘）
- READY 条件满足 → 最终 READY（附回退条件）

Keep it tight. Per-item fields required: 是否为既有冻结规则 / v10 当前状态 / 是否确定回归 / 是否修改 / 修改后的最终结论.

R16 全链路落位一致（A-11 → §2.2 → §13 P0_RULE_IDS → INV-23 → T-17 → 结尾）。

---

# CONTRACT FINAL REGRESSION SCAN

**修复对象**：[Step_7_E_Coding_Contract_v10.md](file:///g:/workspace/zixun4/docs/Step_7_E_Coding_Contract_v10.md)（原地修复 R16，未创建 v11）
**边界确认**：无重新设计、无新增能力、Step 7-D 零改动、扫描项一律只判定不修改。

## 一、R16 恢复：applicable_rules ⊆ P0_RULE_IDS — 已闭合

| 要求 | 落实位置 |
| ---- | ---- |
| `trusted_input.applicable_rules ⊆ P0_RULE_IDS` | [§2.1 A-11](file:///g:/workspace/zixun4/docs/Step_7_E_Coding_Contract_v10.md#L152) |
| P0_RULE_IDS = 冻结 7 条（VALUE.CONSISTENCY / TABLE.INTERNAL / LOGIC.CONSISTENCY / ANCHOR.BINDING / CONCLUSION.DIRECTION / CONCLUSION.COVERAGE / EXPECTED.HIT） | [§13 P0_RULE_IDS](file:///g:/workspace/zixun4/docs/Step_7_E_Coding_Contract_v10.md#L821-L836)（与 P0 表一一对应，受 INV-1 约束） |
| 未知 / 非 7 P0 rule_id → precondition_error | A-11 + [INV-23](file:///g:/workspace/zixun4/docs/Step_7_E_Coding_Contract_v10.md#L61) |
| 不进入 applicable_rule_count | §2.1 说明（§1.3 计数在 Stage A 之后执行） |
| 不静默删除 / 不自动补入 / 不修改 trusted_input | §2.1 说明（只读校验：非法即 fail，不做修正） |
| Stage B 衔接 | [§2.2](file:///g:/workspace/zixun4/docs/Step_7_E_Coding_Contract_v10.md#L177)：membership test 在 A-11 校验后的集合上执行 |
| 测试 | [T-17](file:///g:/workspace/zixun4/docs/Step_7_E_Coding_Contract_v10.md#L880) |

## 二、旧契约遗漏扫描（5 项，只判定不修改）

判定方法说明：v8/v9 契约原文未落盘（磁盘可证），各项“是否为既有冻结规则”以你的指认为准、无法从磁盘独立证实；“是否确定回归”因此一律无法确证 → 全部标记 **REGRESSION CANDIDATE**。其中扫描项 1 已取得 Step 7-D 代码证据（[validator.py](file:///g:/workspace/zixun4/src/eval/validator.py)：`case.validate()` 覆盖 8 类校验，`from_dir()` 不自动调用）。

| # | 扫描项 | 是否为既有冻结规则 | v10 当前状态 | 是否确定回归 | 是否修改 | 修改后的最终结论 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| 1 | case.validate() 纳入 Stage A | 指认为既有；无法独立证实 | 无整体 case 校验；仅 A-7 / A-8 / A-10 局部覆盖。Step 7-D `case.validate()` 的 8 类校验（meta / G3 值与量纲 / G2 / structure / conclusion / known_issues / expected_issues / G3⊆G2）无替代，且 `from_dir()` 不自动调用 | 否——REGRESSION CANDIDATE | 否 | 待冻结登记确认；确认成立 → 下轮以单条 A 项检查恢复，不重设计 |
| 2 | whitelist precondition（pattern / reason / scope / confirmed_by） | 指认为既有；无法独立证实 | §8.4 仅定义匹配语义；Stage A 无登记结构校验，无替代 | 否——REGRESSION CANDIDATE | 否 | 待冻结登记确认 |
| 3 | TrustedTableSpec precondition | 指认为既有；无法独立证实 | `trusted_table_specs` 在 TrustedInput 中但 **TableSpecSnapshot 类型未定义**；Stage A 无检查，无替代 | 否——REGRESSION CANDIDATE | 否 | 待冻结登记确认；另注：类型未定义本身是 P0-2 可执行性缺口（与 B 轮 SystemOutput 同类），确认后一并处理 |
| 4 | renderable_fact_types ⊆ fact_display_spec.keys() | 指认为既有；无法独立证实 | 存在**部分替代**：A-9（元素 ∈ CDM 注册表）+ P0-1 惰性检查（snapshot 缺条目 → precondition_error，标注 v9 冻结保持） | 否——REGRESSION CANDIDATE（部分覆盖 ≠ 明确替代） | 否 | 待确认；若确认，需先裁决「Stage A 集合级校验」与「惰性检查」的替代关系 |
| 5 | G3.unit ∈ UNIT_TO_QUANTITY（或 None） | 指认为既有；无法独立证实（Step 7-D 亦不检查 unit） | A-8 仅校验 fact_id；G3.unit 无合法性检查，无替代 | 否——REGRESSION CANDIDATE | 否 | 待冻结登记确认 |

## 三、READY 条件核对

| 条件 | 结果 |
| ---- | ---- |
| applicable_rules ⊆ P0_RULE_IDS 修复完成 | ✅ R16 全链路落位（A-11 / §2.2 / §13 / INV-23 / T-17） |
| 无未处理的确定性回归 | ✅ 扫描 5 项均为 REGRESSION CANDIDATE（v9 原文缺失，无法确证为确定性回归），已如实列出、零修改、零新增设计 |
| 既有不变量保持 | ✅ R1–R15 全部未触碰；7 P0 / Step 7-D / 六元 identity / matching / truth source 唯一性均保持 |

---

# 最终判定

- R16 恢复完成 ✅
- 扫描 5 项：**0 项确定性回归，5 项 REGRESSION CANDIDATE**（全部待你的冻结登记裁决；确认任一成立 → 回退 NOT READY，下轮以最小 A 项检查恢复）

文档结尾已更新为：

**Step 7-E Coding Contract v10 = READY**

附带明示回退条款：上表 5 项 CANDIDATE 中任何一项被你确认为“既有冻结规则且无明确替代”，契约立即回退 NOT READY。若 5 项裁决完毕均不成立（或均已有替代），READY 即为最终态，可进入 Phase 1 实现。
