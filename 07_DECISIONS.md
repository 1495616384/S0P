# 架构决策记录

> 状态：持续更新
>
> 版本：v0.1
>
> 本文档按【时间 · 意图 · 背景情况】三维结构，按时间顺序记录本项目的架构决策。
>
> 依据：项目规则「修改架构必须记录设计决策」。
>
> 记录原则：
>
> 1. 只记录会影响后续实现的决策，不记录日常实现细节；
> 2. 每条决策必须写明**触发它的背景**，否则后人无法判断该决策是否仍然成立；
> 3. 决策被推翻时不删除原条目，追加新条目说明推翻原因。

---

## 索引

| 编号 | 日期 | 主题 | 状态 |
| ---- | ---- | ---- | ---- |
| D-001 | 2026-09-22 | 建立四层数据模型，禁止大统一模型 | 有效 |
| D-002 | 2026-09-22 | Report IR 先行于渲染实现 | 有效 |
| D-003 | 2026-09-22 | 事实层三态：missing / conflict / rejected，禁止静默覆盖 | 有效 |
| D-004 | 2026-09-22 | 先做无 LLM 骨架 | 有效 |
| D-005 | 2026-09-22 | 段落由「禁止自由文本」改为双模态（Assertion / Narrative） | **推翻 D-002 相关约束** |
| D-006 | 2026-09-22 | 数字锚定校验作为数字红线的实际防线 | 部分成立（集合兜底被 D-017 推翻） |
| D-007 | 2026-09-22 | 新增定性 Fact | 有效 |
| D-008 | 2026-09-22 | Fact 存储改「原始值 + 原始单位 + 量纲种类」，取消 SI 基准 | **推翻原 v0.1 设计** |
| D-009 | 2026-09-22 | 新增 ConclusionRule，结论必须由显式聚合规则产生 | 有效 |
| D-010 | 2026-09-22 | TemplateSpec 同时产出 required_facts[] | 有效 |
| D-011 | 2026-09-22 | 五 Agent 降为 Pipeline Orchestrator + Step | **推翻原 v0.1 设计** |
| D-012 | 2026-09-22 | Evaluation Case 分级 + Ground Truth 独立推导 | 有效 |
| D-013 | 2026-09-22 | Style Validator 基准改为公司标准模板，新增样式使用合规报告 | 有效 |
| D-014 | 2026-09-22 | 删除 Capability Routing | 有效 |
| D-015 | 2026-09-22 | confidence 降为诊断字段，人工闸门改用 review_status | 有效 |
| D-016 | 2026-09-22 | 新增前置任务 0：档案盘点 | 有效 |
| D-017 | 2026-09-22 | 数字锚定改为纯绑定：删除「全文 Fact 值集合」兜底 | 推翻 V2 的锚定规则 |
| D-018 | 2026-09-22 | 锚定范围扩展至实例标识 token 与表格 / 图注数值 | 有效 |
| D-019 | 2026-09-22 | 白名单改为模式级登记制，确认方与生成侧分离 | 有效 |
| D-020 | 2026-09-22 | 引入 Fact 类型层（fact_type + 三段式 id + 受控注册表） | 有效 |
| D-021 | 2026-09-22 | Criterion 与 Fact 同构，未确认不得参与判定 | 有效 |
| D-022 | 2026-09-22 | 回归确定性策略：版本快照 / 温度 0 / 抖动登记 / 禁止重跑到通过 | 有效 |
| D-023 | 2026-09-22 | G4 结论方向必须从 G1 + 已确认 Criterion 推导，否则降级 bronze | 有效 |
| D-024 | 2026-09-22 | gold 经济门禁：gold < 5 时 P0 不阻断构建并标注样本不足 | 有效（阈值待任务 0 校准） |
| D-025 | 2026-09-22 | Ref 格式化单一真相源：改为 TemplateSpec.fact_display_spec[fact_type] | 推翻 V2 的 Ref.format |
| D-026 | 2026-09-22 | IR 稳定 id 规则 + 结论段 covers[] | 有效 |

---

## D-001

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 采用 L1 RawSource → L2 Facts → L3 Domain Objects → L4 Report IR 四层模型，禁止建立同时包含原始数据、事实、工程对象与报告内容的「大统一模型」 |
| **背景** | 项目存在多源异构输入（文字、Excel、图片、规范、模板），如果直接抽取成领域对象，会同时丢失原始定位（不可追溯）与表达自由（模板耦合）。分层是为了让「是什么」与「怎么写」在不同层各自演化 |
| **影响** | `03_CANONICAL_DATA_MODEL.md`、`04_REPORT_IR.md` |
| **是否仍然成立** | 是。v0.2 复核未发现该模型的结构性问题 |

---

## D-002

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | Report IR 必须先于渲染实现定义，且报告正文的数值一律通过 Ref 引用事实 |
| **背景** | 若先写渲染器，业务逻辑会被 Word 的具体版式细节反向污染；若数值允许直接写入正文，则无法追溯、无法验证 |
| **影响** | `04_REPORT_IR.md` |
| **是否仍然成立** | 部分成立。**「数值必须走 Ref」仍然成立；「正文禁止自由文本」已被 D-005 推翻** |

---

## D-003

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | Fact 状态采用 filled / missing / conflict / rejected 四态；多来源不一致时生成 Conflict 记录，禁止「最后写入者胜出」 |
| **背景** | 工程资料常存在多份不一致的来源（设计文件、现场记录、委托单）。静默择一会让错误不可见，而不可见的错误比明显的缺失更危险 |
| **影响** | `03_CANONICAL_DATA_MODEL.md` |
| **是否仍然成立** | 是。v0.2 复核确认为本项目最扎实的设计，予以保留 |

---

## D-004

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 第一阶段必须先实现「输入 → Facts → IR → 渲染 → 输出」的零 LLM 闭环，再逐个节点替换为 LLM |
| **背景** | 若一开始就把 LLM 放进主链路，任何质量变化都无法归因（是模型变了、Prompt 变了还是数据变了）。零 LLM 骨架让 LLM 的引入成为可测量的增量 |
| **影响** | `02_ARCHITECTURE.md` §17 |
| **是否仍然成立** | 是。且 M3 完成时系统零 LLM 可端到端产出可校验报告，是判断架构成立与否的硬标准 |

---

## D-005

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 取消「Paragraph 不允许自由文本」，改为双模态：Assertion 段使用 Lit + Ref；Narrative 段允许受控自然文本，但必须携带 `fact_refs[]` 与 `anchors[]` |
| **背景** | 架构评审指出：真实报告正文包含「斜向裂缝」「判断为受力裂缝」「建议压力灌浆封闭」一类**定性事实与工程判断**，它们既不是静态文本片段，也无 Fact 可指。原约束只有两条出路且都是死路——全塞进 Lit（受控文本名存实亡、断言无法校验），或切成极碎 segments（语言组织能力被废掉）。经复核该论证成立：原设计的实际后果是 AI 系统性滥用 Lit 逃避约束，而 Validator 无法发现 |
| **影响** | `04_REPORT_IR.md` §3.3 / §11–18 / §45；`02_ARCHITECTURE.md` §11.3 |
| **是否仍然成立** | 否，原 v0.1 约束作废 |
| **发起人** | 架构评审 |

---

## D-006

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 数字红线的实际防线由「结构强制」改为「结构强制 + 事后数字锚定」：渲染后提取段落中所有数值 token，每个 token 必须命中本段 anchors、全文 Fact 值集合或显式白名单，未命中即为 P0 错误 |
| **背景** | D-005 放开段落自由文本后，产生新的风险缺口：AI 可以在自然文本里直接写下未经引用的数字。仅靠「允许自由文本 + 事后人工检查」不足以构成防线。锚定校验把「AI 不能编数字」从结构约束转成可执行的校验规则 |
| **影响** | `04_REPORT_IR.md` §41；`02_ARCHITECTURE.md` §11.3；`05_EVALUATION.md` §6.4 |
| **是否仍然成立** | 部分成立。「结构强制 + 事后锚定」的方向仍成立；但其中「命中全文 Fact 值集合即通过」的兜底已被 D-017 推翻，改为「显式绑定 fact_id + 按显示精度值相等」 |
| **发起人** | 架构自查（对评审方案的补强） |

---

## D-007

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 在 Fact Layer 中显式区分**定量 Fact** 与**定性 Fact**。定性 Fact 的值可为受控枚举或短文本（如 `crack.pattern = "斜向"`），并具有与定量 Fact 相同的 source_refs / status / provenance / 人工确认能力 |
| **背景** | D-005 的配套措施。若没有定性 Fact，「受力裂缝」「建议灌浆封闭」这类工程判断只能落在 Lit 或 Prose 中无法校验。引入后，工程判断同样可溯源、可人工确认、可进 Conflict（两位专家结论不同） |
| **影响** | `03_CANONICAL_DATA_MODEL.md`；`04_REPORT_IR.md` |
| **是否仍然成立** | 是 |
| **发起人** | 架构自查（对评审方案的补强） |

---

## D-008

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | Fact 数值存储由「SI 基准 + 原始单位」改为「**原始值 + 原始单位 + quantity_kind（量纲种类）**」。单位换算只发生在计算步骤内；显示一律使用原始单位与原始精度 |
| **背景** | 架构评审指出 SI 存储对工程报告是负优化：报告永远按原始单位呈现（kN、mm、MPa），显示时全部要换算回来，换算链引入舍入误差；`32400000` 这类浮点值在 diff 与回归中制造无意义噪音。经复核成立。**补充**：仅存原始单位会丢失量纲一致性检查能力（mm 被当成 m 相加无法被发现），因此必须同时存 quantity_kind |
| **影响** | `03_CANONICAL_DATA_MODEL.md` §8 / §10；`05_EVALUATION.md` §6.1 |
| **是否仍然成立** | 否，原 v0.1 设计作废 |
| **发起人** | 架构评审 + 架构自查补强 |

---

## D-009

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 新增 `ConclusionRule`：显式定义多项 Evaluation 聚合为单一 Conclusion 的规则（如「任一主控项不合格 → 整体不合格」）。结论必须由该规则程序化产生，禁止 LLM 自由综合 |
| **背景** | 架构自查发现：v0.1 的 `Criterion → Evaluation` 只描述单项判定，但真实报告的结论是多项检测结果的聚合（如「结构安全性等级 B 级」）。缺少聚合层会导致结论章节无法程序化生成，只能交给 LLM，直接击穿「程序负责判定」的红线 |
| **影响** | `03_CANONICAL_DATA_MODEL.md`；`02_ARCHITECTURE.md` D15 |
| **是否仍然成立** | 是 |
| **发起人** | 架构自查（评审未涉及） |

---

## D-010

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | `TemplateSpec` 解析模板时同时产出 `required_facts[]`，用于输入接入阶段的完整性预检 |
| **背景** | 架构自查发现：模板中的固定文字与占位结构实际上定义了「这份报告需要哪些事实」，但 v0.1 只把模板当版式规格，导致缺失数据要等到渲染阶段才逐个暴露。提前到输入阶段可系统性暴露缺失 |
| **影响** | `02_ARCHITECTURE.md` §6.4；`05_EVALUATION.md` |
| **是否仍然成立** | 是 |
| **发起人** | 架构自查（评审未涉及） |

---

## D-011

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 第一阶段删除 IntakeAgent / ExtractionAgent / ReasoningAgent / ReportComposerAgent / ValidatorAgent 五个 Agent，改为 Pipeline Orchestrator + Step。Agent 概念保留为第二阶段演进方向 |
| **背景** | 架构评审指出这是过度 Agent 化：第一阶段主链路是线性 pipeline，不存在需要自主决策的分支——IntakeAgent 是路由函数、ReasoningAgent 职责为空壳（判定已由程序按 Criterion 执行）、ValidatorAgent 是 while 循环。同时与项目规则「第一阶段禁止过度工程化」直接冲突。经复核成立。**保留条件**：Skill 的 schema / version / test_cases 契约必须保留，否则会退化成「一堆普通函数」，丧失可替换与独立回归能力 |
| **影响** | `02_ARCHITECTURE.md` §8；`04_REPORT_IR.md` §45 |
| **是否仍然成立** | 否，原 v0.1 五 Agent 设计作废 |
| **发起人** | 架构评审 |

---

## D-012

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | Evaluation Case 分三级（gold / silver / bronze）；Ground Truth 的事实值必须从原始输入资料独立推导，禁止抄录报告正文；逆向链路与生成链路分离，逆向产物冻结为 JSON 固件；历史报告中的已知错误登记为 known_issues |
| **背景** | 架构评审指出原「历史报告逆向构造评测集」存在三个结构性缺陷：①输入被污染（报告正文含结论信息，测的是复述能力）；②循环验证（同一 LLM 既造题又答题）；③Ground Truth 错误被固化，评测体系反向激励团队复制历史错误。经复核成立。**对评审方案的修正**：评审建议「只有原始输入齐全的项目才进评测集」，但这会使评测集规模被外部档案现状卡死（若只有 2–3 份，回归体系撑不起「换模型仍可靠」的结论），因此改为**分级承担不同评测责任**而非排除 |
| **影响** | `05_EVALUATION.md`；`02_ARCHITECTURE.md` §12 |
| **是否仍然成立** | 是 |
| **发起人** | 架构评审 + 架构自查修正 |

---

## D-013

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | Style Validator 的对比基准由「历史报告」改为「公司标准模板」；验证范围收缩为程序可可靠检查的子集（章节结构、编号、表格完整性、必需要素、样式使用合规）；**新增样式使用合规报告**（实际使用样式名 vs 模板定义样式名，检出硬编码格式覆盖） |
| **背景** | 架构评审指出：①原设计未定义对比基准，若与历史报告比，会把历史报告本身的不规范（不同年代、不同人员、手工调整）固化为标准；②python-docx 系工具对样式继承链、直接格式化覆盖、分节符的解析脆弱，自动验证易退化为大量误报。经复核成立。**对评审结论的修正**：评审认为「模板对了输出就对」——此判断过度乐观，模板保证的是样式**定义**正确，不保证**渲染器使用了样式**（最常见的缺陷恰恰是绕过样式硬编码字体字号），因此必须补样式使用合规检查 |
| **影响** | `05_EVALUATION.md` §6.9；`02_ARCHITECTURE.md` §13.2 |
| **是否仍然成立** | 是 |
| **发起人** | 架构评审 + 架构自查补强 |

---

## D-014

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 删除 LLM Adapter 的 Capability Routing（按能力路由模型），第一阶段只保留 Model Profile 配置文件 |
| **背景** | 架构评审指出第一阶段只会有一个主模型，路由层属过度设计。且 v0.1 自己已写明「不做万能 Provider Framework」，却又定义了 capability routing，自相矛盾。经复核成立 |
| **影响** | `02_ARCHITECTURE.md` §10.3 |
| **是否仍然成立** | 否，原 v0.1 设计作废 |
| **发起人** | 架构评审 |

---

## D-015

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | `confidence` 降为诊断字段（只记录、不驱动决策）；人工确认闸门改用 `review_status ∈ {pending, confirmed, rejected}` |
| **背景** | 架构评审指出 LLM 自报 confidence 与真实正确率相关性极差，用它做人工闸门阈值会制造虚假安全感。经复核成立，属低成本修正 |
| **影响** | `03_CANONICAL_DATA_MODEL.md` §8 / §14 |
| **是否仍然成立** | 是 |
| **发起人** | 架构评审 |

---

## D-016

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 新增前置任务 0：档案盘点。统计历史项目中「原始检测数据 + 文字资料 + 最终报告」三件齐全（gold）的实际数量级，以及 silver / bronze 的分布 |
| **背景** | 架构评审指出：若齐全样本只有 2–3 份，回归体系覆盖面撑不起「更换模型后系统仍然可靠」的结论。这是外部档案现状问题，不是架构缺陷，但必须在建立基线（M7）之前用真实档案回答，否则门禁阈值无法设定。该任务与开发并行，不阻塞 M0–M6 |
| **影响** | `05_EVALUATION.md` §9；`02_ARCHITECTURE.md` §17.3 |
| **是否仍然成立** | 是 |
| **发起人** | 架构评审 |

---

## D-017

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 数字锚定校验由「命中本段 anchors / 全文 Fact 值集合 / 白名单」改为**纯绑定**：每个数值 token 必须显式绑定到 `fact_id` 且按显示精度相等，删除「全文 Fact 值集合」兜底 |
| **背景** | V2 的锚定规则把「值在全文里存在」当成「归属正确」。具体漏洞：报告写「构件 K002 强度为 32.4 MPa」，而 32.4 实际属于 K003——只要该值在全文任何 Fact 中出现即被放行。归属错误（张冠李戴）是工程报告最危险的错误之一，且合计检查与量纲检查均正常，无法发现。锚定若只回答「值存不存在」，就只是幻觉检测器，而不是归属正确性检查器 |
| **影响** | `02_ARCHITECTURE.md` §11.3 / §14 D4；`04_REPORT_IR.md`（anchors 绑定表）；`05_EVALUATION.md` §6.4 |
| **是否仍然成立** | 是。**推翻 V2（D-006）确立的「全文 Fact 值集合兜底」**：D-006 的「结构强制」部分仍有效，其兜底规则作废 |
| **发起人** | 二次评审 + 架构自查 |

---

## D-018

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 锚定对象由「数值 token」扩展为「数值 token + 实例标识 token（构件编号、测点编号等）」；锚定覆盖范围由正文扩展到表格单元格、表注与图片说明 |
| **背景** | 纯数值锚定留下同类漏洞的另一半：编号是实例标识，不是量值。AI 把 K002 的强度值绑到 K003 的编号上，或表格整行错位，此时所有数值都合法，但对象全错，而数值锚定无法察觉。图注 / 表注又最容易被排除在验证范围之外，可它们恰恰是「数值归属」的声明位置（如图注写「测点 S-3 荷载位移曲线」） |
| **影响** | `02_ARCHITECTURE.md` §11.3；`04_REPORT_IR.md`（表格、图注 Inline 的锚定）；`05_EVALUATION.md` §6.4 |
| **是否仍然成立** | 是 |
| **发起人** | 二次评审 + 架构自查 |

---

## D-019

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 白名单由「事后人工兜底」改为**模式级登记制**：以「模式」为单位登记（如固定话术中的年份、规范编号中的编号段），登记项须记录确认方字段；白名单的生成侧与确认侧分离，生成方不得自行登记 |
| **背景** | V2 的白名单是「事后兜底」——锚定不通过就人工加白，等价于把校验器变成可随时绕过的装饰。更危险的是若白名单由生成方自行维护，AI 每编一个数字都能推动白名单扩张，防线会自我瓦解。改为模式级 + 确认方字段后，白名单成为「评审方确认的模式契约」，并可按模式复用（一次确认、全报告生效），而非一次性放行 |
| **影响** | `02_ARCHITECTURE.md` §11.3；`04_REPORT_IR.md`；`05_EVALUATION.md`（白名单登记表） |
| **是否仍然成立** | 是 |
| **发起人** | 二次评审 + 架构自查 |

---

## D-020

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 引入 Fact **类型层**：Fact 采用「三段式 id」（类型 / 实例 / 版本）；`TemplateSpec.required_facts[]` 的条目落在类型层（`fact_type` + `scope` + `cardinality` + `on_missing`），由程序解析到实例 Fact 集合后检查满足性；类型名来自受控的 `fact_type` 注册表（由确定性 Skill 维护） |
| **背景** | V2 的 `required_facts[]` 直接以实例 id 表达需求（D-010），使需求与具体构件强绑定：模板一旦改变构件数量，或换一个项目，需求清单即失效，不同类型报告的需求也无法复用。类型层把「需要哪一类事实」与「哪一份事实」分离，使模板需求跨项目、跨报告类型可复用；受控注册表则防止模板自行发明类型名，避免需求清单退化为自由文本而无法校验 |
| **影响** | `03_CANONICAL_DATA_MODEL.md`（fact_type 注册表、三段式 id）；`02_ARCHITECTURE.md` §6.4 / §17；`04_REPORT_IR.md` |
| **是否仍然成立** | 是。**修正 D-010 的实现方式**：D-010「TemplateSpec 产出 required_facts[]」的意图保留，但其条目由实例 id 改为类型层条目 |
| **发起人** | 二次评审 + 架构自查 |

---

## D-021

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | Criterion（限值）与 Fact 采用**同构**结构，具备 `source_refs` 与 `review_status`；`review_status != confirmed` 的判定依据不得参与判定，对应检测项必须判为 `not_evaluable`，禁止输出方向性结论 |
| **背景** | 判定逻辑是确定性的（D-009），但判定依据——限值——来自 AI 对规范 PDF 的抽取。V2 的 Criterion 既无溯源、也无确认闸门，因此限值抽错一位（0.2mm 抽成 0.5mm）就能翻转判定结论，而 IR 校验、锚定校验、合计检查全部发现不了：它们只检查「用了什么值、值从哪来」，不检查「这个值对不对」。把 Criterion 提升到与 Fact 同等的可溯源 + 可确认级别，判定结论才建立在被确认的依据之上 |
| **影响** | `03_CANONICAL_DATA_MODEL.md`（Criterion Schema）；`02_ARCHITECTURE.md` §11.4；`04_REPORT_IR.md` |
| **是否仍然成立** | 是。**扩展 D-009**：D-009 解决「结论如何聚合」，本条补上「聚合所依据的限值必须先被确认」，二者互补而非推翻 |
| **发起人** | 二次评审 + 架构自查 |

---

## D-022

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 回归测试采用确定性策略：以**版本快照**锁定模型 / Prompt / Skill / 模板版本；LLM 温度设为 0；无法消除的抖动必须**登记**（登记为已知抖动并纳入容忍范围）；**禁止「重跑到通过」** |
| **背景** | 若回归依赖 LLM 随机输出，「今天过了明天不过」会让基线失去意义——分不清是代码回归还是采样抖动，团队会用「再跑一次」自欺欺人，门禁形同虚设。确定性策略把回归的判定对象从「某次运行结果」改为「固定版本 + 固定温度下的产物对比」；抖动登记把不可消除的随机性显式化，而不是靠反复重跑掩盖 |
| **影响** | `05_EVALUATION.md`（回归章节）；`02_ARCHITECTURE.md` §17 |
| **是否仍然成立** | 是 |
| **发起人** | 二次评审 + 架构自查 |

---

## D-023

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 结论方向性评测（G4 级）必须可由 **G1（事实值）+ 已确认 Criterion** 推导得到，否则该用例降级为 bronze，不得计入 G4 结论方向指标 |
| **背景** | 结论方向（合格 / 不合格 / 建议处理）看似文本指标，实际是判定逻辑的产物。若评测用例的结论方向无法从「事实值 + 已确认限值」程序化推导，就等于用历史报告里的一句结论去测系统的另一句结论，测到的是措辞相似度而非判定正确性；更糟的是会把历史报告的错误结论当成标准答案固化。限制 G4 的来源后，结论方向指标才有可验证的推导链 |
| **影响** | `05_EVALUATION.md`（G4 / 分级）；`02_ARCHITECTURE.md` §12 |
| **是否仍然成立** | 是 |
| **发起人** | 二次评审 + 架构自查 |

---

## D-024

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 建立 gold **经济门禁**：当 gold 用例数 < 5 时，P0 指标不阻断构建，但必须在评测报告中标注「样本不足」 |
| **背景** | gold 用例需要「原始检测数据 + 文字资料 + 最终报告」三件齐全，实际档案中数量可能极少。若门禁强行要求少数 gold 全部通过才允许构建，则几份样本的偶然性会决定整个项目是否可交付；若完全不设门禁，又失去回归的意义。经济门禁的方案是：样本不足时降级门禁强度（不阻断）但显式标注，把「信心不足」本身暴露出来，而不是假装信心充足。阈值 5 为暂定值，待任务 0 档案盘点后用真实数量校准 |
| **影响** | `05_EVALUATION.md` §9；`02_ARCHITECTURE.md` §17.3 |
| **是否仍然成立** | 是（阈值待任务 0 校准） |
| **发起人** | 二次评审 + 架构自查 |

---

## D-025

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | Ref 的显示格式化由「Ref.format（每个引用自带格式）」改为 **`TemplateSpec.fact_display_spec[fact_type]`**，即格式由模板按事实类型统一定义，Ref 只负责绑定 `fact_id` |
| **背景** | V2 让每个 Ref 自带 format，导致同一事实在报告不同位置可能被格式化成不同精度（一处 32.4、一处 32.40、一处 32），而锚定校验要求「按显示精度相等」，格式化分散即意味着同类事实显示口径不统一、无法作为回归基线。格式化的真相源必须唯一：由模板（版式层）按 fact_type 统一定义，IR（语义层）只引用事实、不决定显示。这也与 IR 与模板解耦一致——格式属于版式职责 |
| **影响** | `04_REPORT_IR.md`（Ref）；`02_ARCHITECTURE.md`（渲染与锚定）；`03_CANONICAL_DATA_MODEL.md`（fact_type 与显示精度的关系） |
| **是否仍然成立** | 是。**推翻 V2 的 Ref.format 设计**，显示格式真相源统一到 TemplateSpec；D-002「数值必须走 Ref」仍然成立 |
| **发起人** | 二次评审 + 架构自查 |

---

## D-026

| 维度 | 内容 |
| ---- | ---- |
| **时间** | 2026-09-22 |
| **意图** | 规定 Report IR 的**稳定 id 规则**（同一语义对象在多次生成中 id 保持稳定，用于 diff 与回归）；并新增**结论段 `covers[]`**：结论段必须声明其覆盖的检测项 / 判定集合 |
| **背景** | 回归比较依赖 IR 层面的稳定标识，若 id 每次生成重排（如按出现顺序自增），diff 会产生大量伪变更，掩盖真实回归。结论段 `covers[]` 解决另一半问题：报告在结构上「结论覆盖全部检测项」目前只能靠 V1 完整性检查拿外部清单比对，无法自证；由结论段显式声明覆盖面，可将「结论是否遗漏检测项」变为 IR 层可直接校验的断言，对应 V4 的「结论覆盖全部检测项」 |
| **影响** | `04_REPORT_IR.md`（id 规则 / 结论段 covers）；`02_ARCHITECTURE.md` §13.1 |
| **是否仍然成立** | 是 |
| **发起人** | 二次评审 + 架构自查 |

---

## 附录：决策登记模板

新增决策时复制以下结构：

```text
## D-0XX

| 维度 | 内容 |
| ---- | ---- |
| 时间 | YYYY-MM-DD |
| 意图 | 决策内容（要做什么，一句话） |
| 背景 | 触发该决策的具体情况：发现了什么问题、论证是什么、被推翻的旧设计错在哪里 |
| 影响 | 涉及文档与章节 |
| 是否仍然成立 | 是 / 否（否时须注明被哪条决策推翻） |
| 发起人 | 架构评审 / 架构自查 / 用户要求 / 实施反馈 |
```
